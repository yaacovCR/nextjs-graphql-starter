- args:
    sql: CREATE TABLE "public"."user"("email" text NOT NULL UNIQUE, "password" text
      NOT NULL, PRIMARY KEY ("email") );
  type: run_sql
- args:
    name: user
    schema: public
  type: add_existing_table_or_view
